# Tasks

Open work items. Closed items and their history stay in the private repository; the decisions they produced are in DECISIONS.md.

## Open

- [ ] TASK-246 — **Public repository scaffolding (public-release Phase 7, ADR-071)** (opened 2026-08-24). `release/export-public.sh` + `EXCLUDE.txt` + `public-docs.py` + `verify-public.py`; `.github/workflows/ci.yml` and `release.yml`, dependabot, issue/PR templates; optional self-hosted runner (`release/RUNNER.md`, `install-runner.sh`); notary off by default in the chart; `genesis/README.md`. Remaining: dry-run export clean, create the empty GitHub repository, first push with tag, GitHub settings (private vulnerability reporting, branch protection, fork-PR approval), delete the old public repositories.
- [ ] TASK-245 — **Public documentation pass (public-release Phase 6)** (opened 2026-08-24). README rewritten as the public front door; ARCHITECTURE, AGENTS, CONVENTIONS, SECURITY, WHITEPAPER framing, DECISIONS status vocabulary; CONTRIBUTING.md, reports/README.md, audit/README.md new; RUNBOOK.txt split into docs/operators/RUNBOOK.md + private the private runbook; KNOWN-ISSUES / ROADMAP / TESTING / API refreshed; specs status vocabulary unified and token specs marked Reserved; docs/ operator documents rewritten for viper-testnet-1; TASK-243 vocabulary in Ansible and scripts; CONTEXT.md removed, decks moved to the private business folder (private); `scripts/check-links.sh` guards relative links; `make ci` runs every gate.
- [ ] TASK-243 — **Ansible, scripts and docs on the ADR-069 role vocabulary** (opened 2026-08-24). `deploy/ansible` drives everything off `viper_role ∈ {producer, follower}` (inventory examples, `when:` guards in `build-deploy.yml`, `cutover-libp2p.yml`, the `node-config.json.j2` template); `scripts/run_local_devnet.sh`, `configs/README.md`, `specs/consensus.md`, `docs/validator-onboarding.md`, `ROADMAP.md` describe the devnet as producer + followers. The binary reads both names, so nothing breaks; the public repo should speak one language. Do it with the Phase 6 docs rewrite, in one commit per directory.
- [ ] TASK-242 — **Chart renders `node.json` from structured values** (opened 2026-08-24, ADR-069 follow-up). Today the ceremony writes the whole `node.json` into `chainNode.<role>.config.nodeJson`, so the bootstrap multiaddrs carry a release name chosen at ceremony time. Move chain constants, validator set and fee params to `chain.*` values and let `_helpers.tpl` compose the per-role `node.json` (listen field, bootstrap peers from `.Release.Name`, role) at render time; keep `config.nodeJson` as an escape hatch.
- [ ] TASK-241 — **`viper-lab-1` validator is OOM-killed every few hours** (opened 2026-08-24). `pqcd-validator-0` on the 212: 18 restarts, `Last State: OOMKilled`, 1 GiB limit, RSS ~280 MiB shortly after restart and growing with height (4.2 M blocks, every block empty). `compact_chain_to_checkpoint` evicts in-memory blocks at checkpoints, so something else grows — mempool/gossip caches, per-height metrics, RocksDB memtables. Profile with a heap snapshot before `viper-testnet-1` genesis; a chain that must run for years cannot depend on the OOM killer as its GC.
- [ ] TASK-240 — **Node roles in the binary (ADR-069)** (opened 2026-08-24, branch `chore/public-release-prep`, public-release Phase 4). `NodeRole` with the chart vocabulary + aliases, predicates instead of `matches!`, `VIPER_NODE_ID` override, ceremony emitting six role profiles and `_release_name`/`_namespace`, chart render guard + pod-name node ids, `configs/roles/*.json`, chart README and ARCHITECTURE role tables.
- [ ] TASK-239 — **`external_validator_registers_and_participates_in_quorum` is load-sensitive** (opened 2026-08-24). In the first full `cargo test --workspace --all-features` run on this host (3 build jobs + the crate's other tests in parallel, load ~10) the test failed; re-run alone with `--test-threads=1` it passes in 136 s. It spins several in-process nodes over TCP loopback with fixed deadlines (`wait_for_height(.., 5 s)`, `wait_for_convergence_at(.., 8 s)`). Before the public repo runs it in CI, either scale the deadlines with a `PQCD_TEST_TIME_SCALE` env knob or move the multi-node product workflows behind `--test-threads=1` via a `[[test]]` harness setting. Not a consensus bug: no runtime code changed on the branch that surfaced it.
- [ ] TASK-238 — **Quint model: liveness needs a fairness assumption on round increments** (opened 2026-08-24). `specs/bft_consensus.qnt` §4 `proposer()` notes that liveness requires every honest validator to be selected infinitely often, which the abstract model does not yet state as a fairness constraint on round increments. Add the temporal assumption and a liveness property that uses it; Safety/Validity are unaffected. Surfaced by the public-release cleanup (the last TODO marker in a public file).
- [ ] TASK-237 — **Public-release preparation, Phase 3: repository cleanup** (opened 2026-08-24, branch `chore/public-release-prep`). Scope: execute the approved DELETE list (done 2026-08-24, see CHANGELOG *Removed*), redact live-host identifiers from scripts/inventory, review the 22 `allow(dead_code)` sites and the 15 `#[ignore]` tests, run the full suite locally (CI has never run `test:rust` on `main`: it is gated on `RUN_TESTS`), `cargo fmt` / `clippy -D warnings` / `deny` green. Working notes: `the private release plan`, `the private release plan` (untracked).
- [ ] TASK-232 — **Three AGWS landing pages for the new services**. Add `/it/strumenti/timestamp`, `/it/strumenti/evidence`, `/it/strumenti/release` (+ `/en/` mirrors) on `agws-web-consulting`. Pattern: same as existing `/strumenti/*` tool pages. Stealth-mode language (no algorithm names). For now: "preview / request access" form (no live API console). Wired to the existing `/api/contact` endpoint. ~0.5 days. Tracked separately because it lives in the website repo, not the chain repo.
- [ ] TASK-185 — **Post-cutover external operator onboarding validation** (follow-up post-TASK-168). Once devnet-3 is live: have ≥2 external operators complete the full `docs/validator-onboarding.md` flow (keygen → keystore → `register-validator` via TASK-175 CLI → first launch → active-set membership → first elected-proposer round). Record friction points in a debrief under `reports/`. Iterate on the guide. Required precursor to M4 workstream (b) closure — Phase 8 exit criterion (≥5 validators, 7-day soak by 2026-05-20).
- [ ] TASK-186 — **Decidere block_time finale + misurare impatto storage** (MED pre-mainnet). **Tooling landed 2026-05-05**: `scripts/analyze-block-time.py` ingests a JSONL of `pqchain_chain_data_bytes` samples (TASK-187 metric, live since 2026-05-05) and projects daily / weekly / monthly / annual disk growth across the four candidate block_time values 500/1000/2000/5000 ms under the empty-block-chatter linear scaling model (3.3 KB × 3 × commit_signatures × blocks/day; ADR-053 §T1.1). `--metrics-url` mode polls a live `/v1/metrics` endpoint, `--samples-file` mode re-runs against a prior capture, `--self-test` pins the arithmetic + Prometheus metric parsing offline. Captures land in `reports/block-time/<UTC>.jsonl` (gitignored, regenerable; only `reports/block-time/README.md` is tracked, listing the operator workflow + the three orthogonal datasets the ADR-186 author still needs — disk growth from this tool, finality latency from the audit log, throughput ceiling from k6 burst). **Decision still requires soak data**: oggi 500ms block-time → 172.800 blocchi/giorno → ~1,7 GB/giorno SOLO di commit signatures anche a vuoto (3× ML-DSA-65 sig ≈ 10 KB per blocco). Valutare: (a) 1000ms (2× riduzione chatter), (b) 2000ms (4× riduzione), (c) 5000ms (10× riduzione Ethereum-style). Misurare su 7-day soak: chain-growth/giorno, user-visible finality latency, impatto throughput di picco. Deliverable: ADR con valore pinned + dati. Non pre-cutover blocker; pre-mainnet blocker.
- [ ] TASK-118 — Fee revenue data: collect fee metrics over 30-day window (ends 2026-05-20); compare against ADR-022 tokenomics model; open governance proposal if significant deviation
- [ ] TASK-115 — Cryptographic audit engagement: scope `pqc-crypto`, `pqc-state::apply`, `pqc-tx` validation pipeline; produce audit-ready code package per `specs/audit-scope.md`
- [ ] TASK-116 — Dress rehearsal execution: follow `docs/dress-rehearsal-procedure.md` with real validator set; record debrief
- [ ] TASK-218 — **QUIC listener + hybrid PQ TLS verification** (essay items 1+2). Add a libp2p QUIC listener alongside the existing TCP+TLS path; enable X25519MLKEM768 hybrid PQ kex by default (rustls feature); verify ClientHello fragmentation under realistic MTU (the ~1.2 KB hybrid ClientHello can exceed a single QUIC Initial datagram — see `tldr.fail`). Keep TCP+TLS as fallback for 2 weeks of testnet co-existence before flipping QUIC primary. Target: Phase 9.
