# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.83.0"
name = "libp2p-quic"
version = "0.13.0"

# Patch notes (PQ Chain vendor copy, 2026-05-11):
# - Additive injection seam for `rustls::crypto::CryptoProvider` on the QUIC
#   path. Adds:
#       Config::new_with_provider(keypair, provider)
#   plus a private `Self::new_inner(keypair, custom_provider)` helper that
#   `Self::new` and `Self::new_with_provider` both call.
# - Existing `Config::new` keeps upstream 0.13.0 byte-for-byte semantics —
#   it delegates to `new_inner(keypair, None)`.
# - Pairs with the libp2p-tls vendor patch (vendor/libp2p-tls-pq/) — the
#   libp2p-quic config builds two libp2p-tls configs underneath, so its
#   provider injection requires libp2p-tls to expose
#   `make_{client,server}_config_with_provider`.
# - Patch surface: ~30 LOC in src/config.rs only. Other files are
#   byte-identical to upstream.
# - Reason: same as libp2p-tls vendor patch — libp2p-quic 0.13.0 has no
#   injection seam, so consumers cannot enable X25519MLKEM768 on QUIC
#   handshakes. Vendored fork drops the day libp2p/rust-libp2p#6236 lands;
#   see the private design notes
authors = ["Parity Technologies <admin@parity.io>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "TLS based QUIC transport implementation for libp2p"
readme = false
license = "MIT"
repository = "https://github.com/libp2p/rust-libp2p"

[package.metadata.docs.rs]
all-features = true

[features]
tokio = [
    "dep:tokio",
    "if-watch/tokio",
    "quinn/runtime-tokio",
]

[lib]
name = "libp2p_quic"
path = "src/lib.rs"

[[test]]
name = "smoke"
path = "tests/smoke.rs"

[[test]]
name = "stream_compliance"
path = "tests/stream_compliance.rs"
required-features = ["tokio"]

[dependencies.futures]
version = "0.3.30"

[dependencies.futures-timer]
version = "3.0.3"

[dependencies.if-watch]
version = "3.2.1"

[dependencies.libp2p-core]
version = "0.43.1"

[dependencies.libp2p-identity]
version = "0.2.12"

[dependencies.libp2p-tls]
version = "0.6.2"

[dependencies.quinn]
version = "0.11.6"
features = [
    "rustls",
    "futures-io",
]
default-features = false

[dependencies.rand]
version = "0.8.5"

[dependencies.ring]
version = "0.17.12"

[dependencies.rustls]
version = "0.23.9"
default-features = false

[dependencies.socket2]
version = "0.5.7"

[dependencies.thiserror]
version = "2"

[dependencies.tokio]
version = "1.38"
features = [
    "net",
    "rt",
    "time",
]
optional = true
default-features = false

[dependencies.tracing]
version = "0.1.41"

[dev-dependencies.libp2p-identity]
version = "0.2.12"
features = ["rand"]

[dev-dependencies.libp2p-noise]
version = "0.46.1"

[dev-dependencies.libp2p-tcp]
version = "0.44.0"
features = ["tokio"]

[dev-dependencies.libp2p-yamux]
version = "0.47.0"

[dev-dependencies.quickcheck]
version = "1"

[dev-dependencies.tokio]
version = "1.38"
features = [
    "macros",
    "rt-multi-thread",
    "time",
]
default-features = false

[dev-dependencies.tracing-subscriber]
version = "0.3.19"
features = ["env-filter"]

[lints.clippy]
dbg_macro = "warn"
manual_let_else = "warn"
type_complexity = "allow"
unnecessary_wraps = "warn"
used_underscore_binding = "warn"

[lints.clippy.pedantic]
level = "allow"
priority = -1

[lints.rust]
unreachable_pub = "warn"
